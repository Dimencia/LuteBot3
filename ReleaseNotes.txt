Version 3.5.4

- Fixed startup crash for malformed PartitionIndex files
- Track Filtering now opens when a midi is loaded to indicate that adjustments are necessary for most songs to work
- Improved visibility of instrument selection in Track Filtering
- Improved neural network speed via SIMD (fancy superfast thing)
- Improved neural network accuracy via better normalization
- Fixed issue with training UI causing it to report incorrect success rates
- Re-trained default neural network for selecting multiple flute tracks
